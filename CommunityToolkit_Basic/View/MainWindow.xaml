<Window x:Class="CommunityToolkit_Basic.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CommunityToolkit_Basic"
        xmlns:vm="clr-namespace:CommunityToolkit_Basic.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="500">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <StackPanel Margin="10">
        <TextBlock Text="{Binding Text}" Margin="0 0 0 5"/>
        <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 5"/>
        <Button Content="Click!"/>
    </StackPanel>
</Window>
